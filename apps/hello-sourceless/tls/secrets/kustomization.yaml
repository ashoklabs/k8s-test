apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# ksops generator — decrypts *.enc.yaml files at ArgoCD sync time.
# The ksops CMP sidecar must be configured in ArgoCD (see argocd/bootstrap/argocd-values.yaml).
generators:
  - ksops-generator.yaml
