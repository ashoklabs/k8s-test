# Knative net-istio — wires Knative networking through Istio.
# Replaces the default kourier net layer. Depends on both knative-serving-core
# and istio being ready (wave 6).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - https://github.com/knative-extensions/net-istio/releases/download/knative-v1.20.0/net-istio.yaml
